// Package habit holds the icon and colour values the calendar's habit band
// draws with.
//
// Copied from basecamp/hey-cli under the terms in NOTICE, and kept diffable
// against upstream like the rest of the render package. It sits here rather
// than at the top level because the calendar renderer is its only caller.
package habit
